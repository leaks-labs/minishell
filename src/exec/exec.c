#include "exec.h"

t_exec_return	ft_exec_line(t_msh *msh, t_pipeline *pipeline);

t_exec_return	ft_exec_line(t_msh *msh, t_pipeline *pipeline)
{
	(void)msh;
	(void)pipeline;
	return (EXEC_SUCCESS);
}
