#include "exec.h"

t_exec_return ft_exec_line(t_msh *msh, t_cmd_list *cmd_list);

t_exec_return ft_exec_line(t_msh *msh, t_cmd_list *cmd_list)
{
    (void)msh;
    (void)cmd_list;
    return (EXEC_SUCCESS);
}
