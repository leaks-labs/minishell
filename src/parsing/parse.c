#include "parse.h"
#include "exec.h"

t_parse	ft_parse(t_msh *msh, t_pl *pipeline, char *line);

t_parse	ft_parse(t_msh *msh, t_pl *pipeline, char *line)
{
	(void)msh;
	(void)pipeline;
	(void)line;
	return (PARSE_SUCCESS);
}
