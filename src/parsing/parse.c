#include "parse.h"
#include "init.h"

int ft_parse(t_pipeline *pipeline, char *line);

int ft_parse(t_pipeline *pipeline, char *line)
{
    (void)pipeline;
    (void)line;
    return (PARSE_SUCCESS);
}
