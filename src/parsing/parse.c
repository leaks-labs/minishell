#include "parse.h"
#include "init.h"

int ft_parse(t_cmd_list *cmd_list, char *line);

int ft_parse(t_cmd_list *cmd_list, char *line)
{
    (void)cmd_list;
    (void)line;
    return (PARSE_SUCCESS);
}
